import { PoundsToRupeePipe } from './pounds-to-rupee.pipe';

describe('PoundsToRupeePipe', () => {
  it('create an instance', () => {
    const pipe = new PoundsToRupeePipe();
    expect(pipe).toBeTruthy();
  });
});
